# RocSpace / roc-containers / roc-agentsroute Revision — 2026-07-16

## Goal Achieved
- `roc-ai orchestrator <task>` now works (delegates to patched hermes)
- Full ecosystem support for Autonomous Orchestrator mode
- AIS_DEV + Gateway as first-class (gemini-2.5-flash recommended)
- Auto-import agent JSON for AI Studio / AIS-DEV
- All previous fixes preserved

## Key Changes
**roc-containers/**
- apps/ai/ai.sh : added `orchestrator` case + delegation + import
- menu.sh : added option 22 for roc-ai orchestrator
- setup.sh + README.md : updated quickstart + documentation
- new: apps/ai/README-orchestrator.md

**rocspace/**
- packages/shared/src/index.ts : added INFRA_XLOUD + MULTI_ORCHESTRATOR constants
- HANDOFF.md + new REVISION-2026-07-16.md

**roc-agentsroute**
- hermes (already patched): cmd_orchestrator + cmd_import_to_aistudio + AIS_DEV provider + full model listing

## Commands
```bash
roc-ai orchestrator "your complex task"
roc-agent orchestrator "your complex task"
roc-agent import "Agent Name"
hermes import "Agent Name"
```

## Vision
RocSpace = Infrastructure Xloud : Build All Apps + Integrated auto across every provider + Multi-Orchestra + Autonomous Big Scale Models.


---

## v18.0.2 — Source Rebuild Repair (2026-07-16)

Restore v17.1.1 kemarin meninggalkan source gateway **tidak bisa di-rebuild**
(de-bundle tanpa import/export). Diperbaiki penuh:

- Semua modul gateway (`types/utils/ai/clerk/crawl/logs/solace/index.ts`) di-rewire:
  export/import lengkap, zero perubahan logika v17.1.1
- Page HTML kembali ter-bundle (`--loader:.html=text`) + route baru:
  `/dashboard-v18` + `/orchestrator` (UI v18)
- Bundle: 8 KB (broken) → 161 KB (lengkap, 8 HTML pages)
- `deploy-worker.mjs`: strip anotasi TS (`Record<>`, `as any`) — SyntaxError fixed
- `site/wrangler.toml`: main → `src/index.ts` (IIFE dist/index.js tidak work utk ESM)
- tsconfig: rootDir/paths diperbaiki → `tsc --noEmit` pass (gateway, site, shared)
- `shared`: provider union + `'openai'`, AIS_DEV → endpoint baru (jqizmthqeu…)
- Hapus leftover `site/src/index.ts.new-fb` + script deploy workspace mati
- `dashboardStatus`: logs Response kini di-parse (bukan `Response` mentah → `{}`)

⚠️ Deploy tetap via `node scripts/deploy-worker.mjs <site|gateway> --esm`
dengan CF_API_TOKEN — JANGAN lupa re-`wrangler secret put` setelah deploy
(wrangler deploy wipes secrets).

---

## v18.0.2 — Deployment + Integration Fix Live (2026-07-16, sesi 2)

**Deployed ke produksi:**
- `hermes-cloudflare` tag 7d57264a (156.5KB) — versi live `/` = **18.0.2** ✅
- `roc-site` tag 57e02860 (28.2KB)
- Bindings AMAN — diverifikasi sebelum/sesudah deploy (site 1=1, gateway 30=30, tanpa wipe)
- Semua 8 halaman live 200: /dashboard /chat-live /crew /crawl4ai /zapier /logs
  **+ /dashboard-v18 + /orchestrator (baru)**

**Perbaikan integrasi:**
- `pages/*.html` di-un-wrap dari JS bundle (var x_default=) ke raw HTML —
  sebelumnya halaman menyajikan source JS mentah
- `webvirtcloud-firebase-config.json`: rofai-agent → yttriferous-magpie-16ppv (sinkron gateway)
- `webvirtcloud-firebase-bridge.html`: firebaseConfig object + label footer diperbaiki
- Copy inline bridge di `site/src/index.ts` dicek — sudah benar (yttriferous) ✅

**Verifikasi live (masked):**
- Solace: CONNECTED, 5 queues termasuk hermes/agent/orchestrator ✅
- Firebase config di gateway: configured=true, project benar ✅
- Chat E2E via gateway (Bearer + model llama-3.1-8b): 200 ✅
- Oracle VM /health: 5 services (webvirtcloud, postgres, redis, uptime-kuma, npm) ✅
  - Routes via Nginx: /vm/ 200, /wvc/ 200, /kuma/ 200, /monitor/ 302, /vm/novnc/ 405
  - TCP 22/5432/6379 OPEN ✅
- Aiven PG1 + PG2 (AWS Jakarta): TCP OPEN ✅

**Known stale keys di SECRETS-MASTER (update manual):**
- `TOKEN` ≠ produksi (produksi = rocspace2026, terverifikasi 200)
- `TAILSCALE_KEY` kemungkinan expired → tailscale login hang (NeedsLogin)
  → buat auth key baru: login.tailscale.com/admin/settings/keys

**Baru:** `scripts/tailscale-userspace.sh` — join tailnet tanpa root/TUN
(userspace networking + SOCKS5 :1055), berguna utk probe ip tailnet dari CI/sandbox.

---

## Tambahan — VM Health Bridge HTTPS (integrasi "webvirtcloud.ai.studio")

Permintaan user: integrasi Oracle VM ke AI Studio dengan alias
`webvirtcloud.ai.studio`. Subdomain `*.ai.studio` milik Google (tidak bisa
di-klaim user), jadi dibuat integrasi resmi lintas-repo:

**roc-site (deployed, tag baru setelah 57e02860):**
- Route baru `vm.roadfx.biz.id/health` → JSON `/health` VM + CORS `*`.
- Implementasi **raw TCP socket** (`cloudflare:sockets` `connect()` ke
  `161.118.253.28:80`, HTTP/1.1 manual, parse status line + body):
  - Workers `fetch()` ke IP-literal diblokir (CF **error 1003**).
  - Token API tersedia ($CFAT/CFUT/CF_API_TOKEN/CF_TOKEN/CF_AI_TOKEN)
    **tidak punya izin DNS edit** di zone roadfx.biz.id → tidak bisa buat
    record origin (vm-origin) via API.
- Build scripts (`build-site.mjs`, `deploy-worker.mjs`) ditambah
  `--external:cloudflare:sockets`.
- Terverifikasi live 3/3: 200 JSON `{status:ok, host:roc-vm, 5 services}`
  + header CORS lengkap + `no-store`.

**roc-agentsroute (hermes v5.12.0 "Oracle"):**
- `hermes vm status|console|wvc|kuma|monitor|novnc|ssh|tailscale|services`
- Panel "Oracle VM — WebVirtCloud" di dashboard (live status via bridge).
